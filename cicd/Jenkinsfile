pipeline {
    agent {label 'slavenode'}
    environment{
        PATH= "/home/chaitanya/maven/bin:$PATH"
    }
    stages {
        stage('Hello') {
            steps {
               git branch: 'main', changelog: false, poll: false, url: 'https://github.com/Chaitanya0311/pipeline-build.git'
            }
        }
    }
}
